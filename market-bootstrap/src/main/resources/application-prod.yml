spring:
  datasource:
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10

hkd:
  market:
    websocket:
      worker-threads: 16

logging:
  level:
    root: WARN
    com.hkd: INFO
    io.netty: WARN
