spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hkd_market
    username: hkd_admin
    password: hkd_dev_password_2024

  data:
    redis:
      host: localhost
      port: 6379
      password: hkd_redis_2024  # Redis密码

  kafka:
    bootstrap-servers: localhost:9094  # 使用EXTERNAL端口
    # 暂时禁用Kafka listener自动启动
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

server:
  port: 8009  # 使用8009端口避免冲突

hkd:
  market:
    matching-engine:
      websocket-url: ws://localhost:8005/ws/matching

logging:
  level:
    com.hkd: DEBUG
    io.netty: DEBUG
